# Changelog

## 1.0.1

- **Force Save Push** — added a new button to the game detail card (both grid and list views) that manually pushes a game's local save folder to the drive's synced save location. This helps with games whose saves don't get picked up reliably by the normal launch/exit sync, so you can force a backup on demand instead of hoping it synced.
